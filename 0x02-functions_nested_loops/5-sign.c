#include "main.h"

/**
 * print_sign - Checks sign
 * @n: Param
 * Return: Zero
 */
int print_sign(int n)
{
	if (n > 0)
	{
		return (+);
	} 
	else if (n < 0)
	{
		return (-);
	}
	else 
	{
		return (0);
	}
}
